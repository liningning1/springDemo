<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resources">
	<sql id="selectId">
		id,
		name,
		parendId,
		(select r.name from resources r where r.id=resources.parentId) parentName,
		resKey,
		type,
		resUrl,
		level,
		description
	</sql>
	<select id="findAll" resultType="Resources">
		select
		<include refid="selectId" />
		from resources order by level asc
	</select>
	<!-- resultType="Resources" 每返回一条结果封装到Resources里 -->
	<select id="query" resultType="Resources" parameterType="java.util.HashMap">
		select
		<include refid="selectId" />
		from resources
		<where>
			<if test="t.name!=null and t.name!= ''">
				name like '%${t.name}%'
			</if>
		</where>
		order by level asc
	</select>
	<!-- 总的记录数 -->
	<select id="count" parameterType="Resources" resultType="int">
		select count(0) from resources
		<where>
			<if test="name!=null and name!= ''">
				name like '%${name}%'
			</if>
		</where>
	</select>
	<!-- 根椐用户的id获取该用户的权限 此处有争议 等一会测试一下 确定是否正确 -->
	<select id="getUserResources" parameterType="int" resultType="Resources">
		select
		<include refid="selectId" />
		from resources where id in(
		SELECT resc_id FROM
		resources_role WHERE role_id in(
		SELECT role_id FROM user_role
		WHERE user_id =#{id}))
	</select>
	<select id="getResourcesByUserName" parameterType="int"
		resultType="Resources">
		select
		<include refid="selectId" />
		From resources where id in(
		select resc_id from resourecs_role where role_id in(
		select role_id from user,user_role
		where user_id =userId and username=#{username}))
	</select>
	<select id="getById" parameterType="int" resultType="Resources">
		select * from resources where id=#{id}
	</select>
	<!-- 新增 -->
	<insert id="add" parameterType="Resources">
		insert into resources(
		name,
		parentId,
		resKey,
		type,
		resUrl,
		level,
		description)
		values( #{name},
		#{parentId},
		#{resKey},
		#{type},
		#{resUrl},
		#{level},
		#{description})
	</insert>
	<!-- 删除 -->
	<delete id="deleteById" parameterType="int">
		delete from resources
		where id =#{id}
	</delete>
	<update id="update" parameterType="Resources">
		update resources
		set
		name = #{name},
		parentId = #{parentId},
		resKey = #{resKey},
		type = #{type},
		resUrl = #{resUrl},
		level = #{level},
		description = #{description}
		where id = #{id}
	</update>
	<!-- 增加角色和权限 -->
	<insert id="saveRoleResources" parameterType="ResourceRoles">
	    insert into resources_role(role_id,resc_id)values(#{roleid},#{rescid})
	</insert>
    <delete id="deleteRoleResources" parameterType="int">
        delete from resources_role where role_id=#{roleid}
    </delete>
</mapper>